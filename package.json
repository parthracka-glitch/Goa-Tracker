{
  "name": "enquiry-platform-root",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm.cmd install --prefix backend && npm.cmd install --prefix frontend",
    "start": "concurrently \"npm.cmd start --prefix backend\" \"npm.cmd run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
